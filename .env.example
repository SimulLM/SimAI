# Optional runtime overrides. The build scripts do not load this file automatically.
# Export only the variables needed for a local experiment; never commit .env.

# SIMAI_NCCL_VERSION=v2.30
# AS_SEND_LAT=
# AS_NVLS_ENABLE=0
# AS_NVLSTREE_ENABLE=0
# AS_PXN_ENABLE=0
# AS_LOG_LEVEL=INFO

# Required only for physical RDMA mode.
# MPI_INCLUDE_PATH=
# MPI_BIN_PATH=
